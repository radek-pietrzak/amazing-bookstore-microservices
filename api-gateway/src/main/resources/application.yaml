spring:
  application:
    name: api-gateway
  cloud:
      gateway:
        server:
          webflux:
            routes:
              - id: PRODUCT-SERVICE
                uri: lb://PRODUCT-SERVICE
                predicates:
                  - Path=/product-service/**
